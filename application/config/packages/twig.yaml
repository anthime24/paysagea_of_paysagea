twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    form_themes: ['@SonataCore/Form/datepicker.html.twig', 'bootstrap_3_layout.html.twig']
    globals:
        google_api_key: '%env(GOOGLE_API_KEY)%'
