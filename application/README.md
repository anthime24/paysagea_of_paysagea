MonJardinMaTerrasseSf4

traduction du back office
Par défault le langage du backoffice est le français quelque soit l'url ou l'on se trouve .
Pour qu'il soit modifié en fonction de l'url il faut décommenter
    defaults:
            _locale: fr
Dans le fichier config > routes > sonata_admin.yml
